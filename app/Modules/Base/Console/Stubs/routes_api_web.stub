<?php

use {{MODULE_NAMESPACE}}\Http\Controllers\Api\V1\{{MODEL_NAME}}Controller;
use Illuminate\Support\Facades\Route;

Route::middleware(['api'])
    ->prefix('api/v1/web')
    ->name('api.v1.web.')
    ->group(function (): void {
        Route::apiResource('{{ROUTE_SEGMENT}}', {{MODEL_NAME}}Controller::class);
    });
