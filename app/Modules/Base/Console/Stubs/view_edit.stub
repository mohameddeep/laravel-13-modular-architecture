@extends('{{VIEW_NAMESPACE}}::dashboard.{{ROUTE_SEGMENT}}.layout')

@section('title', '{{MODEL_NAME}}')

@section('content')
    <h1>{{ __('Edit') }}</h1>
    <form action="{{ route('dashboard.{{VIEW_NAMESPACE}}.update', ${{MODEL_VARIABLE}}->id) }}" method="post">
        @csrf
        @method('PUT')
        <button type="submit">{{ __('Save') }}</button>
    </form>
@endsection
